/**
 * @knit-helper-4000/engine — pure pattern-generation core.
 *
 * Scaffold only. No engine logic yet: grading, ease, constraints and the row
 * array come later (see BRIEF.md phase plan). This package is intentionally
 * pure — zero I/O, no DOM, no React — so it can later be reused in a desktop
 * build without a rewrite.
 */

export const ENGINE_NAME = 'Knit-Helper 4000 engine';
export const ENGINE_VERSION = '0.0.0';
